# Azure Functions Configuration
# Copy this file to local.settings.json and fill in your actual values
# DO NOT commit local.settings.json to version control!

# Azure Functions Runtime
FUNCTIONS_WORKER_RUNTIME=node
AzureWebJobsStorage=

# Feature Flags - Control which notification channels are enabled
# Set to "true" to enable, "false" or omit to disable
# Useful for testing individual channels or disabling channels in production
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_TEAMS_NOTIFICATIONS=true
ENABLE_SMS_NOTIFICATIONS=true

# SendGrid Email Configuration
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Only required if ENABLE_EMAIL_NOTIFICATIONS=true
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=noreply@procircular.com
IRT_EMAIL_ADDRESS=IRT@procircular.com

# Microsoft Teams Webhook Configuration
# Create webhook in Teams: Channel → More options → Connectors → Incoming Webhook
# Only required if ENABLE_TEAMS_NOTIFICATIONS=true
TEAMS_WEBHOOK_URL=https://yourorg.webhook.office.com/webhookb2/your_webhook_id_here

# Twilio SMS Configuration
# Get credentials from: https://console.twilio.com
# Only required if ENABLE_SMS_NOTIFICATIONS=true
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_FROM_NUMBER=+15551234567
ONCALL_PHONE_NUMBER=+15559876543

# Notes:
# - Phone numbers must be in E.164 format: +[country code][number]
# - SendGrid sender email must be verified in SendGrid dashboard
# - Teams webhook URL must be active (not deleted from Teams)
# - Twilio from number must be a number you own in Twilio
# - You only need to configure credentials for enabled notification channels

